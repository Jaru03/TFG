// Transición de despliegue compartida en el editor (forms inline y expansión
// de preguntas). En su propio módulo para no mezclar constantes con componentes.
export const collapseTransition = { duration: 0.22, ease: [0.16, 1, 0.3, 1] };
